(function(root, unasigned) {
	$.at.toText.datetimeFormat = '{yyyy}/{MM}/{dd} {HH}:{mm}';
	$.at.toText.dateFormat = '{yyyy}/{MM}/{dd}';
	$.at.toText.timeFormat = '{HH}:{mm}';
	$.at.toJSON.datetimeFormat = '{yyyy}-{MM}-{dd} {HH}:{mm}:{ss}';
	$.at.toJSON.dateFormat = '{yyyy}-{MM}-{dd}';
	$.at.toJSON.timeFormat = '{HH}:{mm}:{ss}';
})(this);
